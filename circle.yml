dependencies:
    override:
        - sudo pip install awscli
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync /home/ubuntu/adamwatt.io/.git http://adamwatt.io.s3-website-us-west-2.amazonaws.com --delete