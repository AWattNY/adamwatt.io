dependencies:
    override:
        - sudo pip install awscli
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync https://github.com/AWattNY/adamwatt.io s3://http://adamwatt.io.s3-website-us-west-2.amazonaws.com --delete